export interface AuctionProfile {
  id: string;
  title: string;
  category: string;
  image: string;
  myBid: number;
  currentBid: number;
  timeLeft: string;
  // Agrega aquí otros campos relevantes según tu backend
}
